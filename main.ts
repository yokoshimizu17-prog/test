input.onButtonPressed(Button.A, function () {
    // 0からスタート
    counter += 2 // Aボタンを押したとき、数字が2ずつ増えるように変更
})
/**
 * カウンターを作る
 */
let counter = 0
// 0からスタート
counter += 2
// 1増やす
basic.showNumber(counter)
